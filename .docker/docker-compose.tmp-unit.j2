version: "3.5"

services:
  node-dev:
    build:
      context: ../
      dockerfile: .docker/Dockerfile-chain-dev-unit
      args:
        - "NETWORK={{ NETWORK }}"
    image: node-dev
    container_name: node-dev        

